import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-slider',
  imports: [RouterLink],
  templateUrl: './slider.html',
  styleUrl: './slider.css',
})
export class Slider {}
