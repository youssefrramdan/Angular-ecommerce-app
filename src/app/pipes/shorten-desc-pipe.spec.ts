import { ShortenDescPipe } from './shorten-desc-pipe';

describe('ShortenDescPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenDescPipe();
    expect(pipe).toBeTruthy();
  });
});
